# This is to create decentrailized app called dcord build under solidity 
# it uses hardhat as the contract builder.
# to install hardhat 
`npm install npx`
`npx install hardhat`
`npx hardhat` --> to launch